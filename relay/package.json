{
  "name": "cute-tech-relay",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "mkdir -p data && ADMIN_TOKEN=dev-token workerd serve config.capnp"
  },
  "devDependencies": {
    "workerd": "latest"
  }
}
